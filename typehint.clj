;; Type hints (Metadata) are used to tell the compiler what type the value will be 
;; and allow it to perform type specific optimisations thus potentially making resultant code a bit faster.

(def ^Integer five 5)
